hardware:
  mode: mac        # mac | pi
  camera_enabled: true
  mic_enabled: true
  speaker_enabled: true

recognition:
  threshold: 0.6   # euclidean distance; lower = stricter match. Tune per-install.
