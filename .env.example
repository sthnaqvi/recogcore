# Copy to .env — never commit .env itself.

# Optional override of config.yaml's hardware.mode (mac | pi)
# HARDWARE_MODE=mac

# Only needed if config.yaml's conversation.mode is "llm"
# ANTHROPIC_API_KEY=sk-ant-...
